## Quick Start

#### Start Scala REPL
```bash
docker run -it --rm --name scala \
  andahme/scala
```

#### Start Bash Shell
```bash
docker run -it --rm --name scala \
  --volume ${PWD}:/workspace \
  andahme/scala bash
```


